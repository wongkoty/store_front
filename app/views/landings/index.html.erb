<h1>Mens</h1>
<div class="owl-carousel">
	<% @men_products.each do |product| %>
	<div> <%= link_to image_tag(product.img_url), product_path(product.id) %> </div>
	<% end %>

  <div> Your Content </div>
  <div> Your Content </div>
  <div> Your Content </div>
  <div> Your Content </div>
</div>

<h1>Womens</h1>
<div class="owl-carousel">
	<% @women_products.each do |product| %>
	<div> <%= link_to image_tag(product.img_url), product_path(product.id) %> </div>
	<% end %>

  <div> Your Content </div>
  <div> Your Content </div>
  <div> Your Content </div>
  <div> Your Content </div>
</div>



<h1></h1>


