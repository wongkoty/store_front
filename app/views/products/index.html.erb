<% @products.each do |product| %>
	<%= product.name %></br>
	<%= product.price %></br>
	<%= product.size %></br>
	<%= product.gender %></br>
	<%= link_to image_tag(product.img_url), product_path(product.id) %></br>
<% end %>
